name: iOS Build

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

env:
  NODE_VERSION: '22'

jobs:
  build-ios:
    name: Build iOS IPA
    runs-on: macos-15
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build web assets
        run: npm run build

      # ------------------------------------------------------------------
      # 1. 生成 iOS 工程 (每次从头)
      # ------------------------------------------------------------------
      - name: Add iOS platform (fresh)
        run: npx cap add ios

      # ------------------------------------------------------------------
      # 2. 用 @capacitor/assets 把仓库里的博学者品牌图覆盖 SPM 模板默认占位.
      #    'cap add ios' 打进去的是 ios-spm-template.tar.gz 里自带的通用 AppIcon,
      #    不是博学者品牌图. 必须显式调用 @capacitor/assets generate --ios 来覆盖.
      # ------------------------------------------------------------------
      - name: Generate iOS icon & splash from brand assets
        run: |
          npx @capacitor/assets generate --ios --iosProject ios/App
          # 兜底: 模板 Contents.json 引用的 splash-2732x2732*.png 文件名与
          # @capacitor/assets 生成的 Default@*x~universal*.png 不同, 如果旧文件
          # 残留在 Splash.imageset/ 目录会引起 Xcode warning. 用新的 splash 覆盖
          # 所有 splash-* 文件名就是彻底一致的兜底.
          if ls ios/App/App/Assets.xcassets/Splash.imageset/splash-2732x2732*.png 1>/dev/null 2>&1; then
            for legacy in ios/App/App/Assets.xcassets/Splash.imageset/splash-2732x2732*.png; do
              cp -f resources/splash.png "$legacy"
            done
            echo "[ok] legacy splash-2732x2732 files overwritten with brand splash"
          else
            echo "[ok] no legacy splash files to overwrite"
          fi

      # ------------------------------------------------------------------
      # 3. 同步 web 资源 + 把 dist 拷贝到 public/
      # ------------------------------------------------------------------
      - name: Sync Capacitor
        run: npx cap sync ios

      # ------------------------------------------------------------------
      # 3. 生成 Shared Scheme —— 这是 xcodebuild 的刚性要求
      #    Capacitor 8 + SPM 的模板只把 scheme 放在 xcuserdata/ 下,
      #    CI runner 上看不到, 必须手动创建 xcshareddata/xcschemes/
      # ------------------------------------------------------------------
      - name: Create shared scheme
        run: |
          SCHEMES_DIR="ios/App/App.xcodeproj/xcshareddata/xcschemes"
          mkdir -p "$SCHEMES_DIR"
          cat > "$SCHEMES_DIR/App.xcscheme" <<'XCSHEME_EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <Scheme
             LastUpgradeVersion = "1600"
             version = "1.7">
             <BuildAction
                parallelizeBuildables = "YES"
                buildImplicitDependencies = "YES">
                <BuildActionEntries>
                   <BuildActionEntry
                      buildForAnalyzing = "YES"
                      buildForArchiving = "YES"
                      buildForProfiling = "YES"
                      buildForRunning = "YES"
                      buildForTesting = "YES">
                      <BuildableReference
                         BuildableIdentifier = "primary"
                         BlueprintIdentifier = "504EC3041FED79650016851F"
                         BuildableName = "App.app"
                         BlueprintName = "App"
                         ReferencedContainer = "container:App.xcodeproj">
                      </BuildableReference>
                   </BuildActionEntry>
                </BuildActionEntries>
             </BuildAction>
             <TestAction
                buildConfiguration = "Debug"
                selectedDebuggerIdentifier = "Xcode.DebuggerFoundation.Debugger.LLDB"
                selectedLauncherIdentifier = "Xcode.DebuggerFoundation.Launcher.LLDB"
                shouldUseLaunchSchemeArgsEnv = "YES"
                shouldAutocreateTestPlan = "YES">
                <Testables>
                </Testables>
             </TestAction>
             <LaunchAction
                buildConfiguration = "Debug"
                selectedDebuggerIdentifier = "Xcode.DebuggerFoundation.Debugger.LLDB"
                selectedLauncherIdentifier = "Xcode.DebuggerFoundation.Launcher.LLDB"
                launchStyle = "0"
                useCustomWorkingDirectory = "NO"
                ignoresPersistentStateOnLaunch = "NO"
                debugDocumentVersioning = "YES"
                debugServiceExtension = "internal"
                allowLocationSimulation = "YES">
                <BuildableProductRunnable
                   runnableDebuggingMode = "0">
                   <BuildableReference
                      BuildableIdentifier = "primary"
                      BlueprintIdentifier = "504EC3041FED79650016851F"
                      BuildableName = "App.app"
                      BlueprintName = "App"
                      ReferencedContainer = "container:App.xcodeproj">
                   </BuildableReference>
                </BuildableProductRunnable>
             </LaunchAction>
             <ProfileAction
                buildConfiguration = "Release"
                shouldUseLaunchSchemeArgsEnv = "YES"
                savedToolIdentifier = ""
                useCustomWorkingDirectory = "NO"
                debugDocumentVersioning = "YES">
                <BuildableProductRunnable
                   runnableDebuggingMode = "0">
                   <BuildableReference
                      BuildableIdentifier = "primary"
                      BlueprintIdentifier = "504EC3041FED79650016851F"
                      BuildableName = "App.app"
                      BlueprintName = "App"
                      ReferencedContainer = "container:App.xcodeproj">
                   </BuildableReference>
                </BuildableProductRunnable>
             </ProfileAction>
             <AnalyzeAction
                buildConfiguration = "Debug">
             </AnalyzeAction>
             <ArchiveAction
                buildConfiguration = "Release"
                revealArchiveInOrganizer = "YES">
             </ArchiveAction>
          </Scheme>
          XCSHEME_EOF

          echo "--- Scheme file created ---"
          ls -la "$SCHEMES_DIR/"
          echo "--- Scheme list reported by xcodebuild ---"
          xcodebuild -project ios/App/App.xcodeproj -list

      # ------------------------------------------------------------------
      # 4. 预拉取 SPM 依赖 (capacitor-swift-pm + 本地 capacitor-splash-screen)
      #    GitHub Actions 默认没有 SSH key 访问 github.com, 必须走 https
      #    所以显式传递 -scmProvider system 让 xcodebuild 自己 git clone
      # ------------------------------------------------------------------
      - name: Resolve SPM packages
        run: |
          xcodebuild -resolvePackageDependencies \
            -project ios/App/App.xcodeproj \
            -scheme App \
            -scmProvider system \
            -derivedDataPath ios/App/DerivedData

      # ------------------------------------------------------------------
      # 5. Info.plist 定制（在 archive 之前, 这些值会烘焙进 .app）
      # ------------------------------------------------------------------
      - name: Update Info.plist
        run: |
          PLIST=ios/App/App/Info.plist
          # 版本号必须与 Android 侧 (build.gradle) 同步, 否则双平台版本不一致
          /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString 1.1" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion 2" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :CFBundleDisplayName Boxuezhe" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :CFBundleName Boxuezhe" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :NSAppTransportSecurity dict" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :NSAppTransportSecurity:NSExceptionDomains dict" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :NSAppTransportSecurity:NSExceptionDomains:124.222.199.117 dict" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :NSAppTransportSecurity:NSExceptionDomains:124.222.199.117:NSExceptionAllowsInsecureHTTPLoads bool true" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :NSAppTransportSecurity:NSExceptionDomains:124.222.199.117:NSIncludesSubdomains bool true" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Delete :UISupportedInterfaceOrientations" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :UISupportedInterfaceOrientations array" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :UISupportedInterfaceOrientations:0 string UIInterfaceOrientationPortrait" "$PLIST"
          /usr/libexec/PlistBuddy -c "Delete :UISupportedInterfaceOrientations~ipad" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :UISupportedInterfaceOrientations~ipad array" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :UISupportedInterfaceOrientations~ipad:0 string UIInterfaceOrientationPortrait" "$PLIST"

      # ------------------------------------------------------------------
      # 6. Archive —— unsigned
      # ------------------------------------------------------------------
      - name: Archive (build) unsigned
        run: |
          xcodebuild archive \
            -project ios/App/App.xcodeproj \
            -scheme App \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath "$RUNNER_TEMP/App.xcarchive" \
            -scmProvider system \
            -derivedDataPath ios/App/DerivedData \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO

      # ------------------------------------------------------------------
      # 7. 打 .ipa
      # ------------------------------------------------------------------
      - name: Package .ipa
        run: |
          mkdir -p "$RUNNER_TEMP/ipa/Payload"
          cp -R "$RUNNER_TEMP/App.xcarchive/Products/Applications/App.app" \
                "$RUNNER_TEMP/ipa/Payload/"
          cd "$RUNNER_TEMP/ipa"
          zip -r boschue-unsigned.ipa Payload
          echo "--- IPA contents ---"
          unzip -l boschue-unsigned.ipa | head -30
          echo "--- Done ---"

      # ------------------------------------------------------------------
      # 8. 格式转换 —— 把 unsigned IPA 做 ad-hoc 重签名
      #    用codesign --sign - 做ad-hoc签名, 产出 signed IPA
      #    模拟器 / 越狱机可直接安装, 比普通 zip 打包的 IPA 兼容更好
      # ------------------------------------------------------------------
      - name: Convert format (ad-hoc re-sign)
        run: |
          IPA="$RUNNER_TEMP/ipa/boschue-unsigned.ipa"
          WORK="$RUNNER_TEMP/resign"
          OUT="$RUNNER_TEMP/ipa/boschue.ipa"

          # 解压 IPA
          rm -rf "$WORK"
          mkdir -p "$WORK"
          cd "$WORK"
          unzip -q "$IPA"

          # 对 .app 做 ad-hoc 重签名 (identity = "-" 表示 ad-hoc)
          APP=$(find "$WORK/Payload" -maxdepth 1 -name "*.app" | head -1)
          echo "Signing: $APP"
          codesign --force --deep --sign - "$APP"

          # 重新打包 IPA
          cd "$WORK"
          zip -qr "$OUT" Payload

          # 替换原始 unsigned IPA
          mv "$OUT" "$IPA"
          echo "--- 转换完成 ---"
          ls -la "$RUNNER_TEMP/ipa/"
          echo "--- IPA contents ---"
          unzip -l "$IPA" | head -10

      # ------------------------------------------------------------------
      # 9. 上传
      # ------------------------------------------------------------------
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: boschue-ios
          path: ${{ runner.temp }}/ipa/
          retention-days: 30
